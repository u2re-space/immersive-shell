# Immersive shell content

Web component, immersive shell of `${view}` application

|---------------------------|
|                           |
|       Main process A      |
|                           |
|---------------------------|

## Compatible with

- `tabbed` shell (child-process)
- `window` shell (child-process)
- `minimal` shell (child-process)
- `environment` shell (child-process)
- `${view}` process (contained)
